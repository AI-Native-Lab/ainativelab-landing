# Netlify config for ainativelab.org — one config, two environments by BRANCH.
# Both Netlify sites use base directory = repo root and publish this folder.
# Production site builds the `main` branch; the staging site builds the `staging` branch.
# Every pull request also gets an isolated Deploy Preview — that is the review gate.
[build]
  publish = "."
  command = ""
