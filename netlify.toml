# Netlify config for the PRODUCTION site (ainativelab.org).
# This site's base directory = repo root, so it reads THIS file and publishes live/.
# The staging site uses base directory = staging/ and reads staging/netlify.toml instead.
[build]
  publish = "live"
  command = ""
